//1
#include <iostream>
#include <math.h>
int main()
{
    int a = 2;
    float b = 11.5;
    double y;   
     
    y = (pow((0.1 * log(pow(M_E,3)))/pow(sin((a/b) * M_PI),2)+pow(cos((a/b) * M_PI),2), 1/3));

    std::cout << y;
    return(0);
}
//2
#include <iostream>
#include <math.h>
int main()
{
    int a = 2;
    float b = 11.5;
    double y;   
     
    y = (pow((0.1 * log(pow(M_E,3)))/pow(sin((a/b) * M_PI),2)+pow(cos((a/b) * M_PI),2), 1/3));

    std::cout << y;
    return(0);
}
